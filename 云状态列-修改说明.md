# Explorer++ 「云状态」列 —— 自定义修改说明

本文件记录本 fork 相对官方 Explorer++ 增加的改动:在文件列表里增加一个 **Cloud Status(云状态)** 列,
显示 OneDrive 同步状态(在此设备上可用 / 联机时可用 …),支持**排序/分组**,并显示与 **Windows 资源管理器
"状态"列一模一样的图标**。方便日后查询与继续修改。

---

## 0. 快速信息
- 你的 fork:`https://github.com/wanxin9535/explorerplusplus`
- 本地源码:`E:\ExplorerPP-src`(浅克隆)
- 编好的程序:`E:\Explorer++\Explorer++.exe`(便携版,免管理员)
- 本机没有 C++ 编译环境 → **改完源码后靠 GitHub Actions 云端编译**,不在本地编。

## 1. 如何重新编译(核心流程)
1. 改 `E:\ExplorerPP-src` 里的源码。
2. 提交并推送到 fork 的 master(会触发云端编译):
   ```
   E:\Git\cmd\git.exe -C E:\ExplorerPP-src add -A
   E:\Git\cmd\git.exe -C E:\ExplorerPP-src commit -m "说明"
   E:\Git\cmd\git.exe -C E:\ExplorerPP-src push --force fork master:master
   ```
   (首次推送时 Git Credential Manager 会弹浏览器授权,浏览器已登录 GitHub 就点一下通过。)
3. 打开 `https://github.com/wanxin9535/explorerplusplus/actions`,等 **Build** 工作流里的
   `Build Release / x64` 变绿(约 20–40 分钟,依赖有缓存会更快)。
   - 整个 run 显示 **失败(红叉)是正常的**:最后的 Deploy 步要作者的 AWS 密钥,在 fork 上必然失败,无关。
     只要 `Build Release / x64` 这个 job 绿了、页面底部有 Artifacts 就成功。
4. 在该 run 页面底部 **Artifacts** 下载 `Explorer++.Release.x64`(是个 zip,里面套着
   `explorerpp_x64.zip`=程序、`explorerpp_translations.zip`=各语言 DLL)。
5. 部署:解压,把 `Explorer++.exe` 和 `Explorer++ZH_CN.dll`/`Explorer++ZH_TW.dll` 覆盖到 `E:\Explorer++`。
   注意 exe 和语言 DLL 版本要**配套**(同一次编译出来的)。

## 2. 改了哪些文件(按功能)

### (A) 新增「云状态」列本体
- `Explorer++\Explorer++\ShellBrowser\Columns.h`:枚举加 `CloudStatus = 65`。
- `Explorer++\Explorer++\ShellBrowser\ColumnDataRetrieval.h/.cpp`:
  - `GetCloudStatusColumnText()`:读属性 `PKEY_StorageProviderState` + `PSFormatForDisplayAlloc` → 本地化状态文字。
  - `GetColumnText()` 的 switch 加 `CloudStatus` 分支。
- `Explorer++\Explorer++\ShellBrowser\ColumnHelper.cpp`:`GetColumnName`/`GetColumnDescription` 加分支。
- `Explorer++\Explorer++\resource.h`:`#define IDS_COLUMN_NAME_CLOUDSTATUS 2080`。
- `Explorer++\Explorer++\Explorer++.rc`:英文串 `IDS_COLUMN_NAME_CLOUDSTATUS "Cloud Status"`。
- `Explorer++\Explorer++\DefaultColumns.h`:把 CloudStatus 加进 `REAL_FOLDER_DEFAULT_COLUMNS`(默认不勾)。
- `Explorer++\Explorer++\ColumnXmlStorage.cpp`:列名映射表加 `{ CloudStatus, L"CloudStatus" }`(否则存盘崩)。

### (B) 排序 / 分组
- `Explorer++\Explorer++\ShellBrowser\SortModes.h`:加 `SortMode::CloudStatus = 65`。
- `ColumnManager.cpp` `DetermineColumnSortMode`:CloudStatus → `SortMode::CloudStatus`。
- `SortHelper.h/.cpp`:`SortByCloudStatus()`(按状态文字 `StrCmpLogicalW` 比较)。
- `SortManager.cpp`:排序 switch 加 CloudStatus。
- `SortModeMenuMappings.cpp`:映射表加 `{ IDM_SORTBY_CLOUDSTATUS, SortMode::CloudStatus }`(有 static_assert,漏了编译不过)。
- `SortMenuBuilder.cpp`:`GetSortMenuItemStringIndex` + `DetermineGroupModeMenuId` 各加一分支。
- `GroupManager.cpp`:分组 switch 加 CloudStatus(按状态文字分组)。
- `resource.h`:`IDM_SORTBY_CLOUDSTATUS 50064`、`IDM_GROUPBY_CLOUDSTATUS 50164`。

### (C) 崩溃修复(非英文界面缺列名字符串)
- `Explorer++\Explorer++\Win32ResourceLoader.cpp`:`LoadString`/`MaybeLoadString` 改为
  **翻译 DLL 里找不到该字符串时,回退到 exe 内置英文资源**(根治所有语言缺字符串导致的 CHECK 崩溃 0x80000003)。
- `Translations\Explorer++ZH_CN\Explorer++.rc`:加 `IDS_COLUMN_NAME_CLOUDSTATUS "云状态"`。
- `Translations\Explorer++ZH_TW\Explorer++.rc`:加 `"雲端狀態"`。

### (D) 图标显示(和 Windows 一致)
- `ColumnDataRetrieval.h/.cpp`:
  - `GetCloudStatusValue()`:取 `PKEY_StorageProviderState` 的原始整数值。
  - `ResolveCloudStatusIcon()`:通过属性描述的枚举图标引用
    (`PSGetPropertyDescription → GetEnumTypeList → IPropertyEnumType2::GetImageReference`)
    取出 Windows 给该状态配的**同一套图标**,再 `SHDefExtractIcon` 提取 HICON。
  - `GetCloudStatusIcon()`:按状态值缓存 HICON(进程内 static 缓存)。
- `Explorer++\Explorer++\ShellBrowser\ListView.cpp`:
  - include 加 `ColumnDataRetrieval.h`。
  - `OnListViewCustomDraw`:`CDDS_ITEMPREPAINT` 返回加 `CDRF_NOTIFYSUBITEMDRAW`;
    新增 `CDDS_ITEMPREPAINT | CDDS_SUBITEM` 分支 —— 在云状态列单元格里 `DrawIconEx` 画图标,返回 `CDRF_SKIPDEFAULT`。
  - 取不到图标时返回 `CDRF_DODEFAULT`,自动退回显示文字(不会崩/空白)。

## 3. 如何进一步修改(常见)
- **想默认就显示这列**:`DefaultColumns.h` 里把 CloudStatus 那行的 `FALSE` 改成 `TRUE`。
- **改列显示名**:英文改 `Explorer++.rc` 的 `IDS_COLUMN_NAME_CLOUDSTATUS`;中文改 `Translations\Explorer++ZH_CN\Explorer++.rc` 同名串。
- **图标改大小/位置**:`ListView.cpp` 的 `CDDS_SUBITEM` 分支里 `iconX/iconY`、`DrawIconEx` 的 cx/cy。
- **想连文字一起显示**(图标+文字):把 `CDDS_SUBITEM` 分支结尾的 `CDRF_SKIPDEFAULT` 改成 `CDRF_DODEFAULT`,
  并在画图标后自己把文字往右挪(或调列宽)。
- 改完都要按第 1 节重新编译、下载、覆盖。

## 4. 注意事项 / 坑
- `Explorer++.rc` 及 `Translations\*\Explorer++.rc` 都是 **UTF-16 LE + BOM**,只能按 UTF-16 读写(用 PowerShell
  `[IO.File]::ReadAllText/WriteAllText` + `UnicodeEncoding`),别用普通文本编辑器直接另存成 UTF-8。
- 加新的 `ColumnType`/`SortMode` 时,若有 switch 没有 `default` 且开了 `/WX`,漏写分支会**编译报错**;
  `SortModeMenuMappings.cpp` 还有 `static_assert` 强制每个 SortMode 都要有菜单映射。
- 非英文界面若某字符串只有英文版没翻译,已靠 (C) 的回退机制兜底,不会再崩。

## 5. 版本
- 当前部署:`Explorer++.exe 1.5.0.281x`(见文件属性)。
- 基于官方 master 分支修改。
